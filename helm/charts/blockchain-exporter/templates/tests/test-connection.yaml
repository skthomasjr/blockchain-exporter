apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blockchain-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "blockchain-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blockchain-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
